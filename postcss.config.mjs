const config = {
  theme: {
    extend: {
      colors: {
        primary: "#1D4ED8", // Example: blue color for primary
        secondary: "#D97706", // Example: orange color for secondary
      },
    },
  },
  plugins: ["@tailwindcss/postcss"],
};

export default config;

